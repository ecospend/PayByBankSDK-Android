package com.ecospend.paylinksdk.shared.network.mock

enum class MockAPIType(val api: String, val fileName: String) {
    ExampleService(api = "example/example", fileName = "example_response.json"),
}
